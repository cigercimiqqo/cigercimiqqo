/**
 * Firestore: okuma/yazma işlemleri Firestore Lite ile (edge bundle küçük).
 * onSnapshot abonelikleri firestore-subscriptions modülünde (tam SDK).
 */
export * from './firestore-core';
export { subscribeToSettings, subscribeToOrders } from './firestore-subscriptions';
